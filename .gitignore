# Go build artifacts
/delta
*.test
*.out

# Frontend dependencies and generated output. web/dist is committed so
# `go install` builds embed the frontend; rebuild it before tagging a release.
node_modules/
dist/
frontend/dist/
!/web/dist/

# Local databases and OS metadata
*.db
*.db-*
.DS_Store

# Local configuration
.env
